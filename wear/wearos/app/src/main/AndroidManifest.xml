<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <application
        android:icon="@drawable/ic_rally_launcher"
        android:label="Momentum"
        android:roundIcon="@drawable/ic_rally_launcher"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- Standalone: la partita si può segnare anche senza telefono. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:documentLaunchMode="never"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Riceve startMatch dal telefono anche ad app chiusa. -->
        <service
            android:name=".PhoneListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/rallymate"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <service
            android:name=".MatchWorkoutService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Health Services binds explicitly after the user opts in. The
             service receives low-frequency user-activity transitions; it does
             not poll sensors or keep Momentum alive continuously. -->
        <service
            android:name=".RacketWorkoutDetectionService"
            android:exported="true"
            android:permission="com.google.android.wearable.healthservices.permission.PASSIVE_DATA_BINDING" />

        <receiver
            android:name=".WorkoutDetectionActionReceiver"
            android:exported="false" />
    </application>
</manifest>
